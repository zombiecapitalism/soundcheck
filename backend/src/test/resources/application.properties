# 테스트는 외부 API를 실제로 호출하지 않으므로(CLAUDE.md) 진짜 키가 필요 없다.
# 이 파일이 없으면 SetlistFmProperties의 기동 시 검증 때문에 테스트 컨텍스트 로드가
# 실제 SETLIST_FM_API_KEY 환경변수에 의존하게 된다.
# (application.yml이 아니라 .properties인 이유: 같은 이름의 yml을 test 리소스에 두면
#  classpath 순서상 메인 application.yml 전체가 가려진다. properties는 yml과 병합되고 우선한다.)
setlist-fm.api-key=test-dummy-key
